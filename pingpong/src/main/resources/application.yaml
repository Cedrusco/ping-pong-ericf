app:
  minDelaySeconds: 1
  maxDelaySeconds: 5
  topic:
    pingPong: pingpong
kafka:
  bootstrapServers: localhost:9092
  kafkaAppId: ping-pong
  autoOffsetReset: latest
  teamAGroupId: team-a
  teamBGroupId: team-b