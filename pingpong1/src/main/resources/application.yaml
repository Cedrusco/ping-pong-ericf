app:
  minDelaySeconds: 1
  maxDelaySeconds: 5
  topic:
    ping: topic.ping
    pong: topic.pong
kafka:
  bootstrapServers: localhost:9092
  kafkaAppId: ping-pong
  autoOffsetReset: latest
  transactionalId: ping-pong